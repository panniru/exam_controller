= form_tag documents_path, :method => 'get' , :class => "form-horizontal" do
  .form-group.col-md-4
    = autocomplete_field_tag :search_term,
    '',
    auto_search_autocomplete_document_filename_path,
    :placeholder => "Search", 
    :type => "text",
    :class => "form-control",
    :id_element => "#search"
    = hidden_field_tag :search, :id => "search"
  .col-md-4
    %button.btn.btn-default{:type => "submit",:class => "btn btn-primary"}Search!
    - if current_user.present? and current_user.admin?
      = link_to "Upload Document", new_document_path, :class => "btn btn-primary"
%br


